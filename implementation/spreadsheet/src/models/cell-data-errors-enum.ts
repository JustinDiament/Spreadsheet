/**
 * Represents the possible error types that can be contained in a cell
 */
export enum ErrorCellData {
    INVALREF = "INVALREF",
    INVALOP = "INVALOP",
    INVALFORM = "INVALFORM",
    CIRCREF = "CIRCREF",
    INVALRULE = "INVALRULE",
  }